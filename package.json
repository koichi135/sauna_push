{
  "name": "sauna-push",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "サウナ・コインプッシャー — endless 3D coin pusher set in a sauna",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.20.0",
    "three": "^0.185.0"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
